substitutions:
  device_id: yoann_office_wall_switch
  device_name: yoann-office-wall-switch
  friendly_name: Yoann Office Wall Switch

<<: !include {
  file: "common/networking.yaml",
  vars: {
    device_id: "${device_id}",
    wifi_ssid: !secret wifi_ssid,
    wifi_password: !secret wifi_password,
    wifi_ap_password: !secret wifi_ap_password,
    encryption_key: !secret yoann_office_wall_switch_encryption_key,
    ota_password: !secret yoann_office_wall_switch_ota_password,
  }
}

<<: !include templates/ks-602s.yaml